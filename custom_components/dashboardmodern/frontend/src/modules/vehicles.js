import { el } from "../render/dom.js";
import { renderWidgetLayout } from "../widgets/runtime.js";
import { actionButton } from "./action-state.js";
import { openSharedDetailPanel } from "./detail-panel-controller.js";
import { ENTITY_RE, normalizeMeasurement } from "./shared-measurements.js";
const BAD=new Set(["unknown","unavailable",""]);const now=()=>Date.now();
const get=(r,id)=>ENTITY_RE.test(id||"")?r.getEntityState?.(id):null;
const UNIT={distance:{m:0.001,km:1,mi:1.609344},pressure:{Pa:0.001,kPa:1,bar:100,psi:6.8947572932},current:{mA:0.001,A:1},voltage:{V:1,kV:1000},percent:{"%":1,percent:1},duration:{ms:.001,s:1,sec:1,min:60,h:3600,hr:3600}};
const CONS={"kWh/100km":1,"Wh/km":0.1,"km/kWh":null,"mi/kWh":null};
export function toCelsius(value,from){const v=Number(value);if(!Number.isFinite(v))return NaN;if(from==="C")return v;if(from==="F")return (v-32)*5/9;if(from==="K")return v-273.15;return NaN;}
export function fromCelsius(value,to){const v=Number(value);if(!Number.isFinite(v))return NaN;if(to==="C")return v;if(to==="F")return v*9/5+32;if(to==="K")return v+273.15;return NaN;}
export function convertVehicleUnit(value,from,to,kind){const v=Number(value);if(!Number.isFinite(v))return{available:false,malformed:true,reason:"malformed"};if(kind==="temperature"){const c=toCelsius(v,from),out=fromCelsius(c,to);return Number.isFinite(out)?{available:true,value:out,unit:to,reason:"ok"}:{available:false,malformed:true,reason:"incompatible-unit"};}if(kind==="consumption")return convertConsumption(v,from,to);const t=UNIT[kind];if(!t?.[from]||!t?.[to])return{available:false,malformed:true,reason:"incompatible-unit"};return{available:true,value:v*t[from]/t[to],unit:to,reason:"ok"};}
function convertConsumption(v,from,to){if(!Object.hasOwn(CONS,from)||!Object.hasOwn(CONS,to))return{available:false,malformed:true,reason:"incompatible-unit"};const per100=from==="kWh/100km"?v:from==="Wh/km"?v*0.1:from==="km/kWh"?100/v:from==="mi/kWh"?100/(v*1.609344):NaN;if(!Number.isFinite(per100)||per100<=0)return{available:false,malformed:true,reason:"malformed"};const out=to==="kWh/100km"?per100:to==="Wh/km"?per100*10:to==="km/kWh"?100/per100:to==="mi/kWh"?(100/per100)/1.609344:NaN;return Number.isFinite(out)?{available:true,value:out,unit:to,reason:"ok"}:{available:false,malformed:true,reason:"incompatible-unit"};}
// Colon duration contract: HH:MM and HH:MM:SS, with two-digit minute/second fields.
function parseDuration(raw,unit,{preserveText=false}={}){const text=String(raw??"").trim();let seconds=null;if(typeof raw==="number"||text.match(/^-?\d+(\.\d+)?$/)){const c=convertVehicleUnit(raw,unit||"s","s","duration");seconds=c.available?c.value:null;}else{let m=text.match(/^(\d{1,2}):(\d{2})(?::(\d{2}))?$/);if(m){const mm=Number(m[2]),ss=Number(m[3]||0);seconds=mm>59||ss>59?NaN:Number(m[1])*3600+mm*60+ss;}else{m=text.match(/^(?:(\d+(?:\.\d+)?)h)?\s*(?:(\d+(?:\.\d+)?)m)?$/i);if(m&&(m[1]||m[2]))seconds=Number(m[1]||0)*3600+Number(m[2]||0)*60;else{m=text.match(/^(\d+(?:\.\d+)?)\s*(ms|s|sec|min|m|h|hr)$/i);if(m){const u=m[2]==="m"?"min":m[2],c=convertVehicleUnit(Number(m[1]),u,"s","duration");seconds=c.available?c.value:null;}}}}if(!Number.isFinite(seconds)||seconds<0)return{available:false,malformed:true,reason:"malformed-duration"};if(preserveText&&Number.isNaN(Number(text)))return{available:true,value:text,unit:"text",seconds,reason:"ok"};const out=convertVehicleUnit(seconds,"s",unit||"s","duration");return out.available?{...out,seconds}:{available:false,malformed:true,reason:"incompatible-unit"};}
function timestampState(lastUpdated,staleAfterMs,nowMs,futureToleranceMs){if(!lastUpdated)return{stale:false,malformed:false,reason:"timestamp-missing",time:null};const t=new Date(lastUpdated).getTime();if(!Number.isFinite(t))return{stale:false,malformed:true,reason:"timestamp-invalid",time:null};if(t-nowMs>Number(futureToleranceMs??5000))return{stale:false,malformed:true,reason:"timestamp-future",time:t};if(Number.isFinite(staleAfterMs)&&staleAfterMs>0&&nowMs-t>staleAfterMs)return{stale:true,malformed:false,reason:"timestamp-stale",time:t};return{stale:false,malformed:false,reason:"ok",time:t};}
function base(id,kind,unit,configured=true){return{entityId:id||"",kind,configured,available:false,missing:false,unavailable:false,malformed:false,stale:false,reason:configured?"not-evaluated":"not-configured",rawValue:null,normalizedValue:null,sourceUnit:"",normalizedUnit:unit||"",lastUpdated:null,displayValue:configured?"Unavailable":"Not configured",entityFreshness:null,valueFreshness:null};}
export function normalizeVehicleSource(r,id,{kind="text",unit="",sourceUnitFallback="",staleAfterMs,futureToleranceMs,nowMs=now(),required=false,timestampStalePolicy="entity",coordinateRole="",preserveTextualDuration=false}={}){if(!id&&!required)return base(id,kind,unit,false);const b=base(id,kind,unit,true);if(!id)return{...b,missing:true,reason:"missing-entity-id"};if(!ENTITY_RE.test(id))return{...b,malformed:true,reason:"invalid-entity-id"};const e=get(r,id);if(!e)return{...b,missing:true,reason:"entity-missing"};const raw=e.state,sourceUnit=e.attributes?.unit_of_measurement||sourceUnitFallback||unit||"",lastUpdated=e.last_updated||e.lastUpdated||null,ef=timestampState(lastUpdated,staleAfterMs,nowMs,futureToleranceMs);if(ef.malformed)return{...b,malformed:true,reason:ef.reason,rawValue:raw,sourceUnit,lastUpdated,entityFreshness:ef};if(BAD.has(String(raw??"").trim()))return{...b,unavailable:true,reason:String(raw||"empty"),rawValue:raw,sourceUnit,lastUpdated,entityFreshness:ef};let val=raw,outUnit=unit||sourceUnit,malformed=false,reason="ok",vf=null;if(kind==="power"||kind==="energy"||kind==="percent"){const m=normalizeMeasurement(r,id,{kind,unit:outUnit,staleAfterMs,now:nowMs,futureToleranceMs});return{...b,...m,configured:true,rawValue:raw,normalizedValue:m.normalizedValue,sourceUnit:m.sourceUnit,normalizedUnit:m.normalizedUnit,lastUpdated:m.lastUpdated,stale:m.stale,available:m.available&&!m.stale,displayValue:m.displayValue,entityFreshness:ef,valueFreshness:null};}
if(kind==="duration"){const c=parseDuration(raw,outUnit||sourceUnit||"s",{preserveText:preserveTextualDuration});malformed=!c.available;reason=c.reason;val=c.value;outUnit=c.unit||outUnit||"s";}else if(kind==="timestamp"){const t=new Date(raw).getTime();malformed=!Number.isFinite(t);vf=timestampState(raw,staleAfterMs,nowMs,futureToleranceMs);val=malformed?null:new Date(t).toISOString();outUnit="ISO8601";if(!malformed&&vf.malformed){malformed=true;reason=vf.reason;}else if(!malformed&&vf.stale)reason="timestamp-value-stale";}else if(kind==="coordinates"){val=Number(raw);malformed=!Number.isFinite(val)||(coordinateRole==="latitude"&&(val<-90||val>90))||(coordinateRole==="longitude"&&(val<-180||val>180));reason=malformed?"coordinate-out-of-range":"ok";outUnit="°";}else if(kind==="numeric"){val=Number(raw);malformed=!Number.isFinite(val);reason=malformed?"malformed":"ok";}else if(kind==="consumption"){const c=convertConsumption(Number(raw),sourceUnit,outUnit);malformed=!c.available;reason=c.reason;val=c.value;}else if(["distance","pressure","temperature","current","voltage"].includes(kind)){const c=convertVehicleUnit(raw,sourceUnit,outUnit,kind);malformed=!c.available;reason=c.reason;val=c.value;}else {val=String(raw);outUnit="";}
if(malformed)return{...b,malformed:true,reason,rawValue:raw,sourceUnit,normalizedUnit:outUnit,lastUpdated,entityFreshness:ef,valueFreshness:vf};const policy=kind==="timestamp"?timestampStalePolicy:"entity",stale=policy==="value"?Boolean(vf?.stale):policy==="both"?Boolean(ef.stale||vf?.stale):Boolean(ef.stale);reason=stale?(vf?.stale&&policy!=="entity"?"timestamp-value-stale":"timestamp-stale"):reason;return{...b,available:!stale,stale,reason,rawValue:raw,normalizedValue:val,sourceUnit,normalizedUnit:outUnit,lastUpdated,displayValue:outUnit&&typeof val==="number"?`${val.toFixed(1)} ${outUnit}`:String(val),entityFreshness:ef,valueFreshness:vf};}
const mapKeys=["primaryEntityId","statusEntityId","socEntityId","rangeEntityId","odometerEntityId","chargingConnectedEntityId","chargingStateEntityId","chargingPowerEntityId","chargingEnergyEntityId","currentEntityId","voltageEntityId","chargeLimitEntityId","etaEntityId","latitudeEntityId","longitudeEntityId","locationEntityId","lastUpdateEntityId","tripDistanceEntityId","tripEnergyEntityId","averageConsumptionEntityId","cabinTemperatureEntityId","outsideTemperatureEntityId","climateStateEntityId","lockStateEntityId","trunkStateEntityId"];
export function collectVehicleSources(node,out=[]){if(!node)return out;if(Array.isArray(node)){node.forEach(x=>collectVehicleSources(x,out));return out;}if(typeof node==="object"&&"available"in node&&"reason"in node&&"rawValue"in node){if(node.configured!==false)out.push(node);return out;}if(typeof node==="object")Object.values(node).forEach(x=>collectVehicleSources(x,out));return out;}
function countExcluded(node){if(!node)return 0;if(Array.isArray(node))return node.reduce((n,x)=>n+countExcluded(x),0);if(typeof node==="object"&&"available"in node&&"reason"in node&&"rawValue"in node)return node.configured===false?1:0;if(typeof node==="object")return Object.values(node).reduce((n,x)=>n+countExcluded(x),0);return 0;}
export function aggregateVehicleSources(sources){const vals=collectVehicleSources(sources),availableCount=vals.filter(x=>x.available).length,configuredCount=vals.length,excludedCount=countExcluded(sources);const malformed=vals.some(x=>x.malformed),unavailable=vals.some(x=>x.unavailable),missing=vals.some(x=>x.missing),stale=vals.some(x=>x.stale),partial=availableCount>0&&availableCount<configuredCount,complete=configuredCount>0&&availableCount===configuredCount;const health=malformed?"malformed":unavailable?"unavailable":missing?"missing":stale?"stale":partial?"partial":availableCount>0?"available":"missing";return{health,complete,partial,available:health==="available",missing,unavailable,malformed,stale,configuredCount,availableCount,excludedCount,reason:vals.find(x=>x.malformed||x.unavailable||x.missing||x.stale)?.reason||health,count:configuredCount};}
export function normalizeVehicle(r={},c={}){const units={distance:c.distanceUnit||"km",pressure:c.pressureUnit||"psi",temperature:c.temperatureUnit||"C",current:c.currentUnit||"A",voltage:c.voltageUnit||"V",power:c.powerUnit||"kW",energy:c.energyUnit||"kWh",duration:c.durationUnit||"min",consumption:c.consumptionUnit||"kWh/100km"},o={staleAfterMs:c.staleAfterMs,futureToleranceMs:c.futureToleranceMs};const src=(id,kind="text",unit="",extra={})=>normalizeVehicleSource(r,id,{...o,kind,unit,...extra});const s={primary:src(c.primaryEntityId||c.entityId,"text","",{required:true}),status:src(c.statusEntityId||c.primaryEntityId||c.entityId,"text","",{required:true}),soc:src(c.socEntityId,"percent","%"),range:src(c.rangeEntityId,"distance",units.distance),odometer:src(c.odometerEntityId,"distance",units.distance),chargingConnected:src(c.chargingConnectedEntityId),chargingState:src(c.chargingStateEntityId),chargingPower:src(c.chargingPowerEntityId,"power",units.power),chargingEnergy:src(c.chargingEnergyEntityId,"energy",units.energy),current:src(c.currentEntityId,"current",units.current),voltage:src(c.voltageEntityId,"voltage",units.voltage),chargeLimit:src(c.chargeLimitEntityId,"percent","%"),eta:src(c.etaEntityId,c.etaKind||"duration",units.duration,{sourceUnitFallback:c.etaSourceUnit,preserveTextualDuration:c.preserveTextualDuration}),latitude:src(c.latitudeEntityId,"coordinates","",{coordinateRole:"latitude"}),longitude:src(c.longitudeEntityId,"coordinates","",{coordinateRole:"longitude"}),location:src(c.locationEntityId),lastUpdate:src(c.lastUpdateEntityId,"timestamp","",{timestampStalePolicy:c.lastUpdateStalePolicy||c.timestampStalePolicy||"entity"}),tripDistance:src(c.tripDistanceEntityId,"distance",units.distance),tripEnergy:src(c.tripEnergyEntityId,"energy",units.energy),averageConsumption:src(c.averageConsumptionEntityId,"consumption",units.consumption),cabinTemperature:src(c.cabinTemperatureEntityId,"temperature",units.temperature),outsideTemperature:src(c.outsideTemperatureEntityId,"temperature",units.temperature),climateState:src(c.climateStateEntityId),lockState:src(c.lockStateEntityId),trunkState:src(c.trunkStateEntityId)};const rows=(xs=[],kind="text",unit,extra={})=>xs.map(x=>({id:x.id,title:x.title||x.id,source:src(x.entityId,kind,unit,extra)}));s.doors=rows(c.doors);s.windows=rows(c.windows);s.tyrePressure=rows(c.tyrePressure,"pressure",units.pressure);s.tyreTemperature=rows(c.tyreTemperature,"temperature",units.temperature);s.customMetrics=(c.customMetrics||[]).map(m=>({id:m.id,title:m.title||m.id,source:src(m.entityId,m.kind||"text",m.unit)}));const agg=aggregateVehicleSources(s),primaryAvailable=s.primary.available&&s.status.available,operational=primaryAvailable&&!s.primary.malformed&&!s.primary.missing&&!s.primary.unavailable&&!s.status.malformed&&!s.status.missing&&!s.status.unavailable;return{id:c.id||c.primaryEntityId||c.entityId||"vehicle",title:c.title||s.primary.normalizedValue||"Vehicle",manufacturer:c.manufacturer||"",model:c.model||"",entityIds:Object.fromEntries(mapKeys.map(k=>[k,c[k]||""])),sources:s,...agg,primaryAvailable,operational,actions:c.actions||[],tags:c.tags||[],lowBatteryThreshold:c.lowBatteryThreshold??20};}
export function validateVehicleAction(a={},r={}){const e=[];if(!["service","navigate-section","navigate-view"].includes(a.type))e.push({field:"type",message:"Action type must be explicit."});if(a.type==="service"&&(!a.domain||!a.service))e.push({field:"service",message:"Service action requires domain and service."});if(a.entityId&&!ENTITY_RE.test(a.entityId))e.push({field:"entity",message:"Invalid entity ID."});if(a.target?.entity_id&&!ENTITY_RE.test(a.target.entity_id))e.push({field:"target",message:"Invalid target entity ID."});if(a.visibility?.entityId&&!ENTITY_RE.test(a.visibility.entityId))e.push({field:"visibility",message:"Invalid visibility entity ID."});if(a.data&&typeof a.data!=="object")e.push({field:"data",message:"Action data must be an object."});if(a.disabled!==undefined&&typeof a.disabled!=="boolean")e.push({field:"disabled",message:"Disabled must be boolean."});if(a.confirm!==undefined&&typeof a.confirm!=="boolean")e.push({field:"confirm",message:"Confirmation must be boolean."});if(a.type==="service"&&typeof r.callService!=="function")e.push({field:"runtime",message:"Runtime cannot call services."});return e;}
function visible(a,r){return !a.visibility?.entityId||normalizeVehicleSource(r,a.visibility.entityId).normalizedValue===String(a.visibility.expected??"on");}async function runAction(a,r){if(a.confirm&&globalThis.confirm&&!globalThis.confirm(`Run ${a.title||a.service}?`))return;if(a.type==="navigate-section")return r.navigateToSection?.(a.sectionId);if(a.type==="navigate-view")return r.navigateToView?.(a.viewId);return r.callService?.(a.domain,a.service,{...(a.target||{}),entity_id:a.entityId||a.target?.entity_id,...(a.data||{})});}
const art=c=>el("article",{className:`dm-widget ${c}`});const line=(n,k,s)=>n.append(el("p",{text:`${k}: ${s.displayValue} (${s.reason})`}));const btns=(n,acts,r)=>acts.filter(a=>visible(a,r)).forEach(a=>n.append(actionButton(a.title||a.service||"Action",a.disabled||validateVehicleAction(a,r).length,()=>runAction(a,r),n,"vehicle-action")));
export function renderVehicleTile(w,r={}){const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-tile");n.append(el("h3",{text:w.title||v.title}),el("p",{text:`${v.sources.status.displayValue} · ${v.reason}`}));line(n,"SOC",v.sources.soc);line(n,"Range",v.sources.range);btns(n,v.actions,r);n.append(actionButton("Details",false,()=>openVehicleDetailPanel(w.config,r,n),n,"vehicle-action"));return n;}
export function renderVehicleStatus(w,r={}){const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-status");n.append(el("h3",{text:w.title||"Vehicle status"}));["status","lastUpdate"].forEach(k=>line(n,k,v.sources[k]));return n;}
export const renderVehicleBattery=(w,r={})=>{const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-battery");n.append(el("h3",{text:w.title||"Vehicle battery"}));["soc","chargingPower","chargingEnergy","current","voltage","chargeLimit"].forEach(k=>line(n,k,v.sources[k]));return n;};
export const renderVehicleCharging=(w,r={})=>{const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-charging");n.append(el("h3",{text:w.title||"Vehicle charging"}));["chargingState","chargingPower","eta"].forEach(k=>line(n,k,v.sources[k]));return n;};
export const renderVehicleRange=(w,r={})=>{const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-range");n.append(el("h3",{text:w.title||"Vehicle range"}));["range","odometer"].forEach(k=>line(n,k,v.sources[k]));return n;};
export const renderVehicleTrip=(w,r={})=>{const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-trip");n.append(el("h3",{text:w.title||"Vehicle trip"}));["tripDistance","tripEnergy","averageConsumption"].forEach(k=>line(n,k,v.sources[k]));return n;};
export const renderVehicleTyres=(w,r={})=>{const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-tyres");n.append(el("h3",{text:w.title||"Vehicle tyres"}));[...v.sources.tyrePressure,...v.sources.tyreTemperature].forEach(x=>line(n,x.title,x.source));return n;};
export const renderVehicleLocation=(w,r={})=>{const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-location");n.append(el("h3",{text:w.title||"Vehicle location"}));["latitude","longitude","location"].forEach(k=>line(n,k,v.sources[k]));return n;};
export const renderVehicleClimate=(w,r={})=>{const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-climate");n.append(el("h3",{text:w.title||"Vehicle climate"}));["climateState","cabinTemperature","outsideTemperature"].forEach(k=>line(n,k,v.sources[k]));return n;};
export const renderVehicleSecurity=(w,r={})=>{const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-security");n.append(el("h3",{text:w.title||"Vehicle security"}));line(n,"lock",v.sources.lockState);[...v.sources.doors,...v.sources.windows,{title:"trunk",source:v.sources.trunkState}].forEach(x=>line(n,x.title,x.source));return n;};
function val(x){return x?.normalizedValue??"";}function vehicleCounts(all){return{configured:all.length,available:all.filter(v=>v.available).length,unavailable:all.filter(v=>!v.available||v.unavailable||v.missing).length,charging:all.filter(v=>String(val(v.sources.chargingState)||val(v.sources.status)).toLowerCase().includes("charg")).length,lowBattery:all.filter(v=>Number(val(v.sources.soc))<=v.lowBatteryThreshold).length,unlocked:all.filter(v=>String(val(v.sources.lockState)).toLowerCase()==="unlocked").length};}
export function filteredVehicles(w,r={}){const c=w.config||{},q=String(c.search||"").toLowerCase();let xs=(c.vehicles||[]).map(v=>normalizeVehicle(r,v));xs=xs.filter(v=>(!q||v.title.toLowerCase().includes(q)||v.manufacturer.toLowerCase().includes(q)||v.model.toLowerCase().includes(q))&&(!c.manufacturerFilter||v.manufacturer===c.manufacturerFilter)&&(!c.modelFilter||v.model===c.modelFilter)&&((c.stateFilter||"all")==="all"||val(v.sources.status)===c.stateFilter)&&(!c.chargingOnly||String(val(v.sources.chargingState)||val(v.sources.status)).toLowerCase().includes("charg"))&&(!c.lowBatteryOnly||Number(val(v.sources.soc))<=v.lowBatteryThreshold)&&(!c.unlockedOnly||String(val(v.sources.lockState)).toLowerCase()==="unlocked")&&(!c.tagFilter||(v.tags||[]).includes(c.tagFilter))&&((c.showUnavailable!==false)||v.available));const sort=c.sort||"name";xs.sort((a,b)=>sort==="state"?String(val(a.sources.status)).localeCompare(String(val(b.sources.status)))||a.title.localeCompare(b.title):sort==="soc"?(Number(val(b.sources.soc))||-1)-(Number(val(a.sources.soc))||-1):sort==="range"?(Number(val(b.sources.range))||-1)-(Number(val(a.sources.range))||-1):sort==="lastUpdate"?String(val(b.sources.lastUpdate)).localeCompare(String(val(a.sources.lastUpdate))):a.title.localeCompare(b.title));xs.counts=vehicleCounts((c.vehicles||[]).map(v=>normalizeVehicle(r,v)));return xs;}
export function renderVehiclesOverview(w,r={}){let local={...(w.config||{})};const n=art("dm-vehicles-overview"),list=el("div"),summary=el("p"),search=el("input",{attrs:{type:"search","aria-label":"Search vehicles",value:local.search||""}}),filter=el("select",{attrs:{"aria-label":"Vehicle status filter"}}),sort=el("select",{attrs:{"aria-label":"Sort vehicles"}});for(const [sel,vals,cur]of[[filter,["all","home","away","charging","parked"],local.stateFilter||"all"],[sort,["name","state","soc","range","lastUpdate"],local.sort||"name"]])for(const v of vals){const o=el("option",{text:v,attrs:{value:v}});o.selected=v===cur;sel.append(o);}filter.value=local.stateFilter||"all";sort.value=local.sort||"name";function draw(){local.search=search.value;local.stateFilter=filter.value;local.sort=sort.value;const xs=filteredVehicles({config:local},r),ct=xs.counts;list.replaceChildren();summary.textContent=`${xs.length} shown · ${ct.configured} configured · ${ct.available} available · ${ct.unavailable} unavailable · ${ct.charging} charging · ${ct.lowBattery} low-battery · ${ct.unlocked} unlocked`;xs.forEach(v=>list.append(el("p",{text:`${v.title} · ${v.manufacturer} ${v.model} · ${v.sources.status.displayValue} · ${v.sources.soc.displayValue}`})));if(!xs.length)list.append(el("p",{text:"No vehicles match the current filters."}));}search.addEventListener("input",draw);filter.addEventListener("change",draw);sort.addEventListener("change",draw);n.append(el("h3",{text:w.title||"Vehicles"}),search,filter,sort,summary,list);draw();return n;}
export function renderVehiclePanel(w,r={}){const v=normalizeVehicle(r,w.config||{}),n=art("dm-vehicle-panel");n.setAttribute("role","dialog");n.append(actionButton("Close",false,()=>w.config?.close?.(),n,"vehicle-action"),el("h3",{text:`Vehicle detail: ${v.title}`}),el("p",{text:`Sources: ${Object.values(v.entityIds).filter(Boolean).join(", ")||"none"}`}),el("p",{text:`Status ${v.reason} · available ${v.available}`}));["soc","range","odometer","chargingState","tripDistance","climateState","lockState","latitude","longitude","lastUpdate"].forEach(k=>line(n,k,v.sources[k]));v.sources.tyrePressure.forEach(x=>line(n,`Tyre ${x.title}`,x.source));btns(n,v.actions,r);return n;}
export function openVehicleDetailPanel(config,r={},trigger=null){return openSharedDetailPanel({trigger,label:"Vehicle detail",render:({close})=>renderVehiclePanel({type:"vehicle-control-panel",config:{...config,close}},r)}).panel;}
export function vehiclesSectionRenderer(section,cards,context){const n=el("section",{className:"dashboardmodern-section dm-vehicles-section",attrs:{"data-section-id":section.id}});n.append(el("h3",{text:section.title||"Vehicles"}),renderWidgetLayout(section.config?.widgets||[{id:`${section.id}-vehicles-overview`,type:"vehicles-overview",config:section.config||{},layout:{size:"full"}},{id:`${section.id}-vehicle-status`,type:"vehicle-status",config:{},layout:{size:"medium"}}],context,context.widgetRegistry));return n;}
const cfg=()=>({vehicles:[],actions:[],distanceUnit:"km",pressureUnit:"psi",temperatureUnit:"C",powerUnit:"kW",energyUnit:"kWh"}),one=()=>({actions:[],...cfg()}),validator=c=>Object.values(c||{}).some(v=>String(v).includes("{{"))?[{field:"config",message:"Templates are not supported."}]:[];const widgets=[["vehicle-tile","Vehicle tile",renderVehicleTile,one],["vehicle-status","Vehicle status",renderVehicleStatus,one],["vehicle-battery","Vehicle battery",renderVehicleBattery,one],["vehicle-charging","Vehicle charging",renderVehicleCharging,one],["vehicle-range","Vehicle range",renderVehicleRange,one],["vehicle-trip","Vehicle trip",renderVehicleTrip,one],["vehicle-tyres","Vehicle tyres",renderVehicleTyres,one],["vehicle-location","Vehicle location",renderVehicleLocation,one],["vehicle-climate","Vehicle climate",renderVehicleClimate,one],["vehicle-security","Vehicle security",renderVehicleSecurity,one],["vehicles-overview","Vehicles overview",renderVehiclesOverview,cfg],["vehicle-control-panel","Vehicle control panel",renderVehiclePanel,one]];
export const VEHICLES_MODULE={id:"vehicles",schemaVersion:1,sections:[{type:"vehicles",displayName:"Vehicles",renderer:vehiclesSectionRenderer,defaultConfig:()=>({widgets:[{type:"vehicles-overview",config:cfg(),layout:{size:"full"}},{type:"vehicle-status",config:one(),layout:{size:"medium"}}]})}],widgets:widgets.map(([type,displayName,renderer,defaultConfig])=>({type,displayName,renderer,defaultConfig,validator,schemaVersion:1,supportedLayoutSizes:["small","medium","large","full"]})),editorPanels:[{id:"vehicles-editor",title:"Vehicles editor"}],navigationEntries:[{sectionType:"vehicles",icon:"vehicle",title:"Vehicles"}],actions:["vehicle.service"],detailPanels:[{type:"vehicle-control-panel"}],defaultLayouts:[{sectionType:"vehicles",widgets:["vehicles-overview","vehicle-status"]}]};
