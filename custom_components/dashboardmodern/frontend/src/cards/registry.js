import { el, emptyState } from "../render/dom.js";
const definitions = new Map();
export function assertCardDefinition(definition) { if (!definition || typeof definition !== "object") throw new Error("Card definition is required."); if (typeof definition.type !== "string" || !definition.type.trim()) throw new Error("Card definition type must be a non-empty string."); if (typeof definition.displayName !== "string" || !definition.displayName.trim()) throw new Error(`Card definition ${definition.type} needs a display name.`); if (typeof definition.renderer !== "function") throw new Error(`Card definition ${definition.type} needs a renderer.`); }
export function registerCardType(definition) { assertCardDefinition(definition); if (definitions.has(definition.type)) throw new Error(`Card type already registered: ${definition.type}`); definitions.set(definition.type, Object.freeze({ ...definition })); return definition; }
export function getCardType(type) { return definitions.get(type) || null; }
export function listCardTypes() { return [...definitions.values()].sort((a, b) => a.displayName.localeCompare(b.displayName)); }
export function clearCardRegistryForTests() { definitions.clear(); }
export function renderUnknownCard(card) { const shell = el("article", { className: "dashboardmodern-card legacy-card", attrs: { "data-card-kind": "unknown", "data-card-id": card?.id || "", "data-unsupported-card-type": card?.type || "" } }); shell.append(el("h4", { text: card?.title || "Unsupported card" })); shell.append(el("p", { className: "dashboardmodern-card-type", text: `Card type: ${card?.type || "unknown"}` })); if (card?.config && typeof card.config === "object" && !Array.isArray(card.config)) { const keys = Object.keys(card.config).sort(); shell.append(el("p", { text: keys.length ? `Configuration keys: ${keys.join(", ")}` : "No card configuration." })); } shell.append(emptyState("This card type is not registered yet. Its JSON config remains editable.")); return shell; }
